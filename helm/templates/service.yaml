apiVersion: v1
kind: Service
metadata:
  name: {{ include "wazuh-agent-daemonset.fullname" . }}
  labels:
    {{- include "wazuh-agent-daemonset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wazuh-agent-daemonset.selectorLabels" . | nindent 4 }}
