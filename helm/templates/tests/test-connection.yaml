apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wazuh-agent-daemonset.fullname" . }}-test-connection"
  labels:
    {{- include "wazuh-agent-daemonset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wazuh-agent-daemonset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
